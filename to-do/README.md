# GAS 移植 作業フェーズ

Python 版（`app/`）を Google Apps Script へ移植するための作業単位。
**「Phase1 を実装して」と指示すれば、そのファイルだけを読んで着手できる**ように書いてある。

| Phase | 内容 | 依存 | 状態 |
|---|---|---|---|
| [Phase1](Phase1.md) | 共通基盤（設定・時刻・流量制御・認可・HTTPクライアント） | なし | ✅ **完了**（2026-09-02。`runCoreTests` 22件通過 / `checkSetup()` を実環境で確認） |
| [Phase2](Phase2.md) | 状態管理と実行基盤（シート・Properties・ロック・コミット点） | Phase1 | ✅ **完了**（2026-09-04。`runAllTests` 55件通過 / `initSheets()` と `runDummyCycle()` を実環境で確認） |
| [Phase3](Phase3.md) | 要件1 `career_status` ＋ Slack 通知 | Phase2 | ✅ **完了**（2026-09-05。`runAllTests` 88件通過 / 実環境で通し確認。残1: コード値→ラベルの実機確認） |
| [Phase4](Phase4.md) | 要件2/3 `progress_flow` | Phase3 | ✅ **完了**（2026-09-06。`runAllTests` 120件通過 / 実環境で通し確認。持ち越した2項目は Phase5 で解消） |
| [Phase5](Phase5.md) | 運用（トリガー本設定・日次サマリ・通し確認） | Phase4 | ✅ **完了**（2026-09-06。`runAllTests` 148件通過 / 自動運転を実環境で確認。仕様書 11.13節） |
| [Phase6](Phase6.md) | 要件4 `career_action_watch` ＋ メール送信 | Phase2 | ✅ **完了**（2026-09-08。`runAllTests` 179件通過 / 自動運転を実環境で確認。仕様書 11.14 / 11.15節） |

## 進め方の原則

- **順番に進める。**Phase3 以降は前の Phase の完了条件が満たされていることが前提。
  **Phase1〜6 すべて完了。要件1〜4 が GAS 上で自動運転している。**
  残っているのは各 Phase の「残っていること」（差出人の変更・本番接続時の実測など）。
- **各 Phase の「完了条件」を満たさずに次へ進まない。**
  特に Phase1・Phase2 は全機能が乗る土台なので、ここが緩いと後で全部やり直しになる。
- **仕様書（`docs/CP進捗通知_システム仕様書.md`）が唯一の根拠。**
  GAS 固有の差分は 11章。1〜10章は特記なき限りそのまま適用される。
- 各 Phase の「やらないこと」を守る。先回りして実装しない。

## 全 Phase 共通の制約（毎回確認すること）

1. **CP API は 1分間に 240 リクエストを超えない。**全 HTTP 呼び出しが単一のトークンバケットを通る。
   （`rules/20-rate-limit.md`。これが最重要制約）
2. **CP に書き戻さない。**読み取り専用。
3. **秘密情報を `gas/src/` に置かない。**API キーと Slack Webhook URL は
   スクリプトプロパティに手で設定する（`rules/40-secrets-and-security.md`）。
4. **Apps Script のエディタで直接編集しない。**`clasp push` が唯一の反映経路。
5. 個人情報をログに出さない。項目 ID とリソース ID のみ。

# 最終確認と作動方法

## 1. 起動方法
自動運転を開始したいときは、`setup.gs` 内の `createTriggers()` を実行して自動運転に乗せます。  
登録後、`showTriggers()` を実行することで、以下の4つのトリガーが正しく常時監視（登録）されているか確認できます。

### 「常時監視」の正確な意味
プロセスが動き続けているのではなく、**決まった間隔で起動して数秒で終わる**を繰り返します。  
`showTriggers()` は「トリガーが正しく登録されていること」を確認するものであり、常時動作し続けているプロセス自体を監視しているわけではありません。

---

## 2. 常時監視トリガー一覧

| 関数 | 間隔 | 1回の中身 |
| :--- | :--- | :--- |
| `runProgressFlow` | 5分 | 要件2/3。進捗履歴の追加を拾う |
| `runCareerStatus` | 15分 | 要件1。求職者229項目の変化を拾う |
| `runCareerAction` | 15分 | 要件4。対応履歴の登録・更新・完了を拾ってメール |
| `runDailySummary` | 毎日7時 | 前日ぶんの集計を `ops` チャンネルへ |

---

## 3. `showTriggers()` で確認できるチェック項目

さらに以下の状態も `showTriggers()` で確認できます。

- **`duplicated` が空**：同じ関数が2本回っていないか（2本あると流量が倍になります）
- **`unmanaged` が空**：画面から手で足したトリガーが無いか

---

## 4. 実行の仕様・制限事項

- **排他制御（`LockService`）**  
  2本が同時に走ることはありません。`LockService` で排他制御しており、前の実行が終わっていなければ後発はすぐ抜けます（ログに `lock_busy` が出力されます）。
- **リクエスト流量・予算**  
  合計流量も **60 req/分** の設計上限を超えません。要件4が加わって **1日あたり約 1,150 リクエスト**、想定予算 5,000 の **23% 程度** となります。
- **反映時間差**  
  時間差は **10分程度** は覚悟してください。

---

## 5. 停止方法

止めたいときは `setup.gs` 内の **`deleteTriggers()`** を実行します。
