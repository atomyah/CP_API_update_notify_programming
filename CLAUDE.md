# CP進捗通知 — CAREER PLUS API 連携 PoC

CAREER PLUS（以下 CP）の API v2 をポーリングし、求職者の状態変化を Slack / メールで通知する PoC。

## 現在のフェーズ

**実装フェーズ（2026-08-07 〜）。要件1・2・3 は実装・実環境動作確認まで完了。**

| 要件 | 状態 |
|---|---|
| 要件1 `career_status` | ✅ **実装済み・実環境動作確認済み**（共通基盤 `app/core/` と Slack 通知を含む） |
| 要件2/3 `progress_flow` | ✅ **実装済み・実環境動作確認済み**（2026-08-09。名前解決 `app/core/resolver.py` を含む） |
| 要件4 `career_action_watch` | ⏸ メール送信がペンディング（仕様書 8.4） |

**実装の根拠は `docs/CP進捗通知_システム仕様書.md`。**
`docs/design/` は検討経緯であり、食い違う場合は仕様書が正。

```powershell
py -3 -m app.main --check                                # 疎通・権限・項目IDの検証のみ
py -3 -m app.main --bootstrap                            # 基準の構築（通知しない）
py -3 -m app.main --once --watcher progress_flow         # 1サイクルだけ実行
py -3 -m app.main --once --dry-run                       # 通知を ops チャンネルへ寄せる
py -3 -m pytest tests -q                                 # 117 tests
```

実環境検証は完了済み。4要件すべてが実現可能であることを実測で確認している。

APIキーは発行済み（`.env` の `CP_NOTIFY_API_KEY`）。読み取り専用。検証テナントに接続できる。

| 検証 | 結果 |
|---|---|
| V-1 オリつく項目 | ✅ **成立**。`CAREER#48002`（国籍）。要件1は完全に実現可能 |
| V-2 求人紹介OK→新規登録 | ✅ **進捗ステータスの遷移だった**（`16` → `11`）。**要件2と1ウォッチャーに統合** |
| V-3a/b 関連リソース | ✅ 成立。要件4の宛先取得が1リクエストで済む |
| V-3c 対応履歴で親の更新日が動くか | ❌ **動かない**。要件4は戦略B（ID集合の差分＋日付窓）で確定 |
| V-4 / V-5 / V-6 / V-7 | ✅ 完了 |

実装前に確定が要るもの（アーキテクチャは変わらないが設定値が決まらない）:

- **1日あたりの新規対応履歴件数**（C-11）。要件4の走査量 `W` を決める唯一の未確定パラメータ。
  本番接続時に `career_action/search { ACTION_DATE GE 昨日, limit:1 }` の `count` を7日ぶん取る
**⏸ 要件4のメール送信はペンディング**（C-2 再、2026-08-07）。
アプリパスワードが組織全体で無効化されており当初の案A・案Bとも使えないため、
送信経路の決着もローカル SMTP でのテストも当面行わない。
`career_action_watch.enabled` の既定は `false`。
**要件1〜3 の実装と、要件4の検知ロジックの実装はブロックしない。**
再開時の手順と管理者依頼文は仕様書 8.4 / 付録C。

解決済み: 対応履歴の総件数（約30,000）/ 担当者未設定時の扱い（送らず日次サマリ）/
Slack Webhook 4本（`.env` に設定済み）/ サーバの設置先（**PoC は手元 Windows PC。常時起動しない**）。

詳細は `docs/design/06-open-questions.md`、統合仕様は `docs/CP進捗通知_システム仕様書.md`。

例外として書いてよいもの:
- `tools/docs_dump/` — 原典を読み直すための解析スクリプト
- `tools/verify/` — 実環境検証の使い捨てスクリプト。`app/` の実装ではない
- `tools/gas_test/` — `gas/src` のテストを push せずに node で走らせる
  （`node tools/gas_test/run_tests.js`）。`clasp` の `rootDir` の外なので push されない

## GAS 移植（2026-09-02 〜）

Python 版（`app/`）を Google Apps Script へ移植する。**根拠は仕様書 11章。**
1〜10章の記述は特記なき限り GAS 版にもそのまま適用される。

| 項目 | 決定 |
|---|---|
| ソース | `gas/src/`。`clasp` でデプロイ（`clasp push` が唯一の反映経路） |
| バインド先 | スプレッドシートのコンテナバインドスクリプト（`gas/.clasp.json` の `scriptId`） |
| 状態管理 | `cursors` は **PropertiesService**、`snapshots` / `notified` / `dead_letter` は **スプレッドシート** |
| `snapshots` | **1求職者 = 1行、監視項目 = 列。**`getValues()` / `setValues()` 各1回で読み書きする |
| コミット点 | **カーソルの書き込みを1サイクルの最後の単一操作にする**（トランザクションがないため） |
| 排他・時間 | `LockService`。1回の実行は最長6分。**経過時間でも中断する** |

**⚠️ Apps Script のエディタで直接編集しない。**`clasp push` の内容が失われる。

**⚠️ `notified` の UNIQUE 制約が失われる**（シートに一意制約はない）。二重通知の防止はアプリ側で持つ。移植で最も事故になりやすい箇所。

**作業フェーズは `to-do/` に分割してある**（Phase1〜6・索引は `to-do/README.md`）。「Phase5 を実装して」で着手できる。
**Phase1〜4 は完了。要件1・2・3 が GAS 上で動作している**（仕様書 11.9節 / 11.10節 / 11.11節）。
Phase5 に持ち越した確認: トリガー2本を同時に動かしたときの `LockService` と合計流量。

```powershell
node tools/gas_test/run_tests.js                         # GAS の単体テスト 120件（push 不要）
```

未確認: GAS の日次クォータ（`UrlFetchApp` 呼び出し数・トリガー合計実行時間）。仕様書 11.7節 / C-15。

## ドキュメントの読み方

| パス | 内容 |
|---|---|
| `docs/CAREER_PLUS_API_仕様書_v2.0.4 .html` | **原典**。API Blueprint 生成物 |
| `docs/CAREER PLUS_APIv2_項目一覧_v3.14.7.xlsx` | **原典**。リソース別の項目ID一覧 |
| `docs/APIキー作成時の操作許可リスト.pdf` | **原典**。テキストレイヤなしの画像PDF |
| `tools/docs_dump/` | 上記3点をテキスト化する解析スクリプトと出力 |
| `docs/design/` | 設計書一式（下記） |

原典を再確認するときは `tools/docs_dump/README.md` の手順で再生成する。
生成物（`tools/docs_dump/out/`）は原典ではない。**仕様の根拠を述べるときは必ず原典の記述に当たること。**

## 設計書

| ファイル | 内容 |
|---|---|
| `docs/design/00-findings.md` | 原典から確認した事実と、事前調査に対する訂正・補足 |
| `docs/design/01-architecture.md` | アプリ構成、共通基盤、ウォッチャー実行モデル |
| `docs/design/02-watchers.md` | 要件1〜4のウォッチャー個別設計 |
| `docs/design/03-rate-budget.md` | 240req/分制約に対する流量設計と予算 |
| `docs/design/04-verification-plan.md` | 実環境で最初に検証すべきこと（実装可否の分岐点） |
| `docs/design/05-apikey-request.md` | APIキー作成リクエスト（必要権限） |
| `docs/design/06-open-questions.md` | 未決事項・要ヒアリング事項 |
| **`docs/design/07-verification-results.md`** | **実環境検証の結果。原典と食い違う箇所はこちらが正** |
| `docs/design/08-ui-operation-request.md` | 画面操作のお願い（V-2 / V-3c） |

## 決定済みの前提

| 項目 | 決定 |
|---|---|
| 実行環境 | Python・単一プロセス。**PoC は手元 Windows PC で断続起動**（仕様書 9.5節） |
| 状態管理 | SQLite（単一ファイル） |
| 監視ルールの設定 | YAML 設定ファイル直編集（エンジニアが変更） |
| 許容遅延 | 要件ごとに個別設定（5分／15分／60分） |
| CP への書き込み | **一切しない**。読み取り専用の API キーで運用する |
| 要件1の対象求職者 | **全求職者**（2026-08-07 に「アスカ1名のみ」から拡大）。絞るなら `target.condition` を足す |
| 要件1の監視項目 | **schema の全項目 229件**（232 − 除外3）。オリつく項目 `CAREER#48002`（国籍）を含む。**値は限定せず、変化したら通知** |
| 要件1の監視リソース | **求職者（`career`）のみ。**企業・求人・進捗・部署・ファイルは監視しない（2026-08-07 の決定） |
| 要件2と要件3 | **同一データソース。1ウォッチャーに統合**（要件3 = ステータス `16`→`11` の遷移） |
| 要件2の通知範囲 | **モードA（全遷移を通知）で開始。**マスタの返却順は業務フロー順ではないため、フロー定義を推測で埋めない。実データを見てから `watched_statuses` で絞る |
| 要件2の遷移前ステータス | **CP は返さない。**1つ前に観測した値を `snapshots` に持ち越す（追加リクエスト0）。初回観測時は `(不明)`。**冪等キーには入れない**（オーバーラップで二重通知になる） |
| 要件4のトリガー | **`ACTION_DATE` / `COMPLETE_DATE` / `NEXTACTION_DATE` の3日付のみ。**本文の変更は通知しない |
| 要件4の通知本文 | アクション種別・メモ本文・求職者名（トリガーではなく表示内容） |
| 要件4の宛先 | **`CAREER#CHARGE_EMAIL`**（求職者の担当者）。**空なら送らない**（件数を集計して日次で報告） |
| 要件4の走査窓 | 新規検知 30日 / 変更検知 **3日** |
| 全件走査 | **行わない。**走査量は総件数（3万件）ではなく直近の活動量に比例させる |
| 想定流量 | **約 5,000 req/日（平均 3.5 req/分）**。CP 上限 240 req/分 の 1.5% |

### 検証用レコード

| 求職者 | ID | 国籍(`CAREER#48002`) | 担当者メール |
|---|---:|---|---|
| 惣流 アスカ | 18 | 米国 | a.yahara@mybrainlab.net |
| 葛城 ミサト | 17 | 日本 | a.yahara@mybrainlab.net |

## 絶対に守る制約

1. **1分間に240リクエストを超えると API サービスを停止される可能性がある**（仕様書「利用方法」に明記）。
   これが設計上の最重要制約。全 HTTP 呼び出しは単一のトークンバケットを通す。
2. **CP に書き戻さない**。通知専用。作成/更新/削除の権限は API キーに付与しない。
3. **API キーをブラウザに渡さない**（仕様書に明記）。サーバサイドでのみ保持する。
4. **Webhook / イベント通知は存在しない**。全要件がポーリング＋差分検知になる。
5. **失敗したらカーソルを進めない**。1つのウォッチャーの失敗が他を巻き込まない。

## ルール

詳細な作業ルールは `rules/` を参照。

@rules/00-scope-and-phase.md
@rules/10-cp-api.md
@rules/20-rate-limit.md
@rules/30-state-and-idempotency.md
@rules/40-secrets-and-security.md
@rules/50-code-style.md

## 開発環境

- Python は `py -3`（3.11.9）で起動する。`python` は Windows Store のスタブなので使わない。
- シェルは PowerShell 5.1。`&&` / `||` は使えない。
