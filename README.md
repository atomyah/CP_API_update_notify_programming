# CP進捗通知

CAREER PLUS（CP）の API v2 をポーリングし、求職者の状態変化を Slack / メールで通知する。

Webhook もイベント通知も存在しないため、全要件がポーリング + 差分検知で成立している。

## 要件と状態

| 要件 | 内容 | 状態 |
|---|---|---|
| 要件1 | 求職者の項目（オリつく項目を含む229件）が変わったら Slack へ | ✅ 稼働 |
| 要件2 | 進捗フローが進行したら Slack へ | ✅ 稼働 |
| 要件3 | 「求人紹介OK」を専用チャンネルへ | ✅ 稼働（要件2と同一データソース） |
| 要件4 | 対応履歴の登録・更新・完了を担当者へメール | ✅ 稼働（`MailApp`） |

## 実装

| | |
|---|---|
| `app/` | **Python 版。**要件1〜3 が実装済み・実環境で動作確認済み |
| `gas/src/` | **Google Apps Script 版（移植先）。**要件1〜4 が自動運転中（トリガー4本） |

GAS 版は1つのスプレッドシートのコンテナバインドスクリプトで、`clasp push` が唯一の反映経路。
状態は `cursors` を PropertiesService、`snapshots` / `id_sets` / `notified` / `dead_letter` を
シートに持つ。

**要件4のメールは `MailApp` で送る**（Python 版がペンディングだった SMTP の前提が
GAS では消える）。2026-09-08 に実機で送受信を確認済み（日次上限 1,500通）。
手順は [`to-do/Phase6.md`](to-do/Phase6.md)。

```powershell
py -3 -m pytest tests -q            # Python 版の単体テスト（117件）
node tools/gas_test/run_tests.js    # GAS 版の単体テスト（179件。clasp push 不要）
```

どちらのテストも **CP API・Slack・メール・本番のシートを一切叩かない。**

## 設計上の最重要制約

1. **1分間に240リクエストを超えると API サービスを停止される可能性がある。**
   全 HTTP 呼び出しを単一のトークンバケットに通し、平常時は 60 req/分 に抑える。
   実測は約 860 req/日・ピーク 12 req/分。
2. **CP に書き戻さない。**API キーは読み取り専用。
3. **失敗したらカーソルを進めない。**取りこぼし（通知漏れ）は重複通知より重いため、
   オーバーラップして再取得し、冪等キーで重複を除去する。

## ドキュメント

| パス | 内容 |
|---|---|
| [`docs/CP進捗通知_システム仕様書.md`](docs/CP進捗通知_システム仕様書.md) | **唯一の根拠。**1〜10章が本体、11章が GAS 移植 |
| [`to-do/README.md`](to-do/README.md) | GAS 移植の作業フェーズ（Phase1〜6）と進捗 |
| [`docs/design/`](docs/design/) | 設計書。検討経緯であり、仕様書と食い違う場合は仕様書が正 |
| [`rules/`](rules/) | 作業ルール（スコープ・CP API・流量・状態管理・秘密情報・コード方針） |
| [`CLAUDE.md`](CLAUDE.md) | 開発時の前提のまとめ |

原典（CP の API 仕様書・項目一覧・権限リスト）は `docs/` に置いてある。
**仕様の根拠を述べるときは生成物ではなく原典に当たること**（`tools/docs_dump/README.md`）。

## 秘密情報

API キーと Slack Webhook URL はリポジトリに置かない。
Python 版は `.env`（`.env.example` を参照）、GAS 版はスクリプトプロパティから読む。
